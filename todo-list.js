class ToDo {
	constructor(id, title) {
		this.id = id
		this.title = title;
		this.tasks = [];
		this.urgent = false;
	}
	// ***Functions****
	saveToStorage() {
		// add to localStorage
	}
	deleteFromStorage() {
		// remove from local storage
	}
	updatedTodo() {
		// push to array
	}

// ***Closing brace***
}